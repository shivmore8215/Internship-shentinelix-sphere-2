#!/bin/bash
echo "Starting Bash Keylogger Simulation..."
echo "Logging keys to keylog_bash.txt"

# Simulate a process that stays alive
while true; do
    # In a real scenario, this would capture input.
    # Here we just sleep to simulate a running process.
    sleep 1
done
